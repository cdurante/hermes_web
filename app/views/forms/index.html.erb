<h1>Listing forms</h1>

<table>
  <thead>
    <tr>
      <th>Formkey</th>
      <th>Dateshared</th>
      <th>Duedate</th>
      <th>Source</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @forms.each do |form| %>
      <tr>
        <td><%= form.formkey %></td>
        <td><%= form.dateshared %></td>
        <td><%= form.duedate %></td>
        <td><%= form.source %></td>
        <td><%= link_to 'Show', form %></td>
        <td><%= link_to 'Edit', edit_form_path(form) %></td>
        <td><%= link_to 'Destroy', form, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Form', new_form_path %>
