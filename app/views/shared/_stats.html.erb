<% @user ||= current_user %>
<div class="stats">
 <% if @user.physician == true %>
  <a href="<%= following_user_path(@user) %>">
    <strong id="Patients" class="stat">
      <%= @user.followed_users.count %>
    </strong>
    Patients
  </a>
  <% end %>
  
  <a href="<%= followers_user_path(@user) %>">
    <strong id="Physicians" class="stat">
      <%= @user.followers.count %>
    </strong>
    Physicians
  </a>
</div>



