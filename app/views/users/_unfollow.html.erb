<%= form_for(current_user.relationships.find_by(followed_id: @user.id),
			html: { method: :delete },
			remote: true) do |f| %>
  <%= f.submit "Remove from My Patients", class: "btn btn-large" %>
<% end %>
